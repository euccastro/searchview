name: root
enabled: yes
layout: fill
children:
    - name: controls
      class: searchview.control
      enabled: yes
      layout: bottom height//8
    - id: view1
      class: searchview.view
      enabled: yes
      history: test.history
      layout: fill

